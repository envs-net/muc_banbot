[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: opt-in tests that require a live XMPP/Prosody environment
    omemo: tests for OMEMO glue and policy; skipped if optional dependencies are missing
    rtbl: tests for RTBL PubSub handling
addopts = -ra
